@startuml

    !include ../lexakai/lexakai.theme
    
    title "com.telenav.kivakit.build.metadata"
    
    class BuildName
    {
    }
    
    Metadata --> Map : "creates"
    class Metadata
    {
        --(collection)--
        +Map<String,String> buildProperties()
        +Map<String,String> projectProperties()
        --(metadata)--
        +Metadata of(Class<?> projectType)
    }
    
    class MetadataUpdater
    {
        --
        +void main(String[] arguments)
    }
    
    enum Metadata.Type
    {
        CURRENT
        PROJECT
    }
    
@enduml

